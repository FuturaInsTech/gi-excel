<!DOCTYPE html>
<html>
<head>
  <title>Group Quote Info</title>
  <style>
      h3 {
        text-align: center;
      }
      table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
        border: 1.5px solid white;
      }

      td,
      th {
        text-align: left;
        padding: 8px;
        border: 1.5px solid white;
      }

     
      .box {
        border: 2px solid #000;
        padding: 5px;
        width: 100%;
        box-sizing: border-box;
        margin-bottom: 10px;
      }

      .invoice-table {
        width: 100%;
        border-collapse: collapse;
        border: 1px solid black; /* Outer border of the table */
      }
      .invoice-table th {
        background-color: #007BFF; /* Blue background for header */
        color: white; /* White text color for contrast */
        border: 1px solid black;
        padding: 8px;
        text-align: center;
      }
      .invoice-table td {
        border: 1px solid black; /* Borders between cells */
        padding: 8px;
        text-align: center;
      }
      .invoice-table td.text-right {
        text-align: right;
      }
      .invoice-record-title {
        text-align: center;
        font-weight: bold;
        margin: 20px 0;
      }
      .invoice-table  tr:nth-child(even) {
        background-color: #f2f2f2; /* Light grey background for even rows */
      }

  </style>
</head>
<body>
  <h1 style="text-align: center;">{{.CompanyData.CompanyName}}</h1>
  <h4 style="text-align: center;">
    {{.CompanyData.CompanyAddress1}}, 
    {{.CompanyData.CompanyAddress2}}, 
    {{.CompanyData.CompanyAddress3}}, 
    {{.CompanyData.CompanyPostalCode}}
  </h4>
  <br />
  <br />
  <br />
<h3>INVOICE REFERENCE NO: {{.ReferenceId}}</h3>

 
  <table>
    <tr>
      <td style="text-align: left;">Bdm Name : {{.BdmName}}</td>
      <td style="text-align: right;">Quotation ID : {{.QuotationID}}</td>
    </tr>
    <tr>
     <td style="text-align: left;">Agent Name : {{.AgentName}}</td>
      <td style="text-align: right;">GrossPremium : {{.GrossPremium}}</td>
    </tr>
    <tr>
      <td style="text-align: left;">Employer Name : {{.EmployerName}}</td>
    </tr>
  </table>

  <br />
  <p class="invoice-record-title">INVOICE RECORD</p>
  <table class="invoice-table">
    <thead>
      <tr>
        <th>Effective Date</th>
        <th>System Date</th>
        <th>Reference No</th>
        <th>Invoice Amount</th>
      </tr>
    </thead>
    <tbody>
      {{range .Invoice}}
      <tr>
        <td>{{.EffectiveDate}}</td>
        <td>{{.SystemDate}}</td>
        <td>{{.ReferenceNo}}</td>
        <td class="text-right">{{.InvoiceAmount}}</td>
      </tr>
      {{end}}
    </tbody>
  </table>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
---------------------------------------------------------------------------------------------------------------------------------------------------------------

  <br />
  <br />
</body>
</html>
